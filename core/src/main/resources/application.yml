spring:
  profiles:
    active: default
  application:
    name: tabletop-games-shop
  datasource:
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    url: ${DB_URL}
  flyway:
    default-schema: public
  jpa:
    hibernate:
      ddl-auto: validate

server:
  port: 9999

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}

azure:
  services:
    storage:
      connectionString: ${BLOB_URL}
      containerName: ${BLOB_CONTAINER_NAME}
